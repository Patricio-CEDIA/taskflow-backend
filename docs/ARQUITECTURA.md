# Arquitectura de TaskFlow

> Este archivo es parte de la Tarea 2 del curso (se asigna al cierre de la Sesión 2, se entrega en la Sesión 4).

## Diagrama

{{REEMPLAZAR: pega aquí tu diagrama de arquitectura (imagen o descripción en texto) — React (Vite) ⇄ HTTP/JSON ⇄ Laravel API ⇄ MySQL}}

## Decisiones de arquitectura

- **Arquitectura desacoplada (API-first):** {{REEMPLAZAR: explica en 2-3 líneas por qué TaskFlow usa un backend Laravel puramente API en vez de renderizar vistas con Blade}}
- **Capas del backend:** {{REEMPLAZAR: describe brevemente qué vive en cada capa — presentación (rutas/controladores), lógica de negocio (modelos/servicios), acceso a datos (Eloquent/migraciones)}}
